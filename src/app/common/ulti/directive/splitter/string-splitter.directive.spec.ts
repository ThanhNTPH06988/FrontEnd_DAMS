import { StringSplitterDirective } from './string-splitter.directive';

describe('StringSplitterDirective', () => {
  it('should create an instance', () => {
    const directive = new StringSplitterDirective();
    expect(directive).toBeTruthy();
  });
});
